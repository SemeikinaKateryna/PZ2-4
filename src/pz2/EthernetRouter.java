package pz2;

public class EthernetRouter extends AdslRouter{
    public EthernetRouter(int maxSpeed, double wifiFrequency, int numberOfAntennas,
                          String color, int weight, String cable) {
        super(maxSpeed, wifiFrequency, numberOfAntennas, color, weight,cable);
    }
}
